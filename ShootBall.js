class ShootBall{
    constructor(bodyA, pointB){
        var options = {
            bodyA: bodyA,
            pointB: pointB,
            stiffness: 0.04,
            length: 10
        }

    }

 body = Bodies.rectangle(x, y, 20, height, option.width) = 20
.height = height
.Body.setAngle(this.body, angle)
add(world,body)

display()
var pos =this.body.position;
var angle  = this.body.angle;
push();
translate(pos.x, pos.y);
rotate(angle);
rectMode(CENTER);
strokeWeight(2);
rect(0, 0, this.width, this.height);
pop();

